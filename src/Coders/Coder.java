package src.Coders;

/**
 * Class that encodes or decodes between a JSON string and an object
 */
public class Coder {

    /**
     * Function to encode an object and turn it into a JSON format
     * @param o an object that can be represented as JSON
     * @return returns a JSON format string
     */
    String encode(Object o) {
        return null;
    }

    /**
     * Function that takes a JSON in string type and returns an object
     * @param json a JSON type string
     * @return an object
     */
    Object decode(String json) {
        return null;
    }
}
