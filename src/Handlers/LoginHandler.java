package src.Handlers;

import src.Result.LoginResult;
import src.Request.Request;

/**
 * Class that handles the load service
 */
public class LoginHandler extends Handler{


    /**
     * Function that will call the login Service, handle the errors, and create a result
     * @param body the body of the request
     * @return the result of the login service
     */
    public LoginResult login(Request body) {
        return null;
    }
}
